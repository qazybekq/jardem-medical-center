# 🔧 Git Sync Troubleshooting Guide

## Проблема: Данные не сохраняются после перезапуска

Если вы добавили пациента или прием, но после перезапуска они исчезли, проверьте следующее:

### 1. Проверка настроек в Streamlit Cloud

Убедитесь, что в **Secrets** вашего приложения установлены:

```toml
GIT_SYNC_ENABLED = "true"
GIT_USER_NAME = "Streamlit Cloud"
GIT_USER_EMAIL = "your-email@example.com"
GIT_BRANCH = "main"
GIT_REMOTE = "origin"
```

### 2. Проверка прав доступа Git

Streamlit Cloud должен иметь права на **запись** в репозиторий:
- Репозиторий должен быть подключен к Streamlit Cloud
- У приложения должны быть права на push

### 3. Проверка логов

В логах Streamlit Cloud ищите сообщения:
- `✅ Successfully synced to Git` - синхронизация успешна
- `⚠️ Failed to push to Git` - ошибка синхронизации
- `Git sync is disabled` - синхронизация отключена

### 4. Ручная проверка

Запустите скрипт проверки:
```bash
python3 check_git_sync.py
```

### 5. Возможные проблемы и решения

#### Проблема: Git push fails with authentication error
**Решение:** Убедитесь, что репозиторий правильно подключен к Streamlit Cloud

#### Проблема: Git push fails with "non-fast-forward"
**Решение:** Система автоматически делает pull перед push, но если проблема сохраняется, проверьте конфликты

#### Проблема: Данные сохраняются локально, но не в Git
**Решение:** 
- Проверьте, что `GIT_SYNC_ENABLED = "true"`
- Проверьте логи на наличие ошибок
- Убедитесь, что база данных не в .gitignore

### 6. Альтернативное решение

Если Git синхронизация не работает, рассмотрите:
- Использование облачной БД (PostgreSQL, MySQL)
- Использование облачного хранилища (S3, GCS) для базы данных
- Регулярное резервное копирование

### 7. Проверка после изменений

После добавления пациента или приема:
1. Подождите 2-3 секунды (синхронизация синхронная)
2. Проверьте логи Streamlit Cloud
3. Проверьте коммиты в GitHub - должны быть новые коммиты с сообщениями "Auto-commit: ..."

### 8. Принудительная синхронизация

Если нужно принудительно синхронизировать:
```python
from git_sync import sync_database_to_git_sync
sync_database_to_git_sync("Manual sync", push=True)
```

