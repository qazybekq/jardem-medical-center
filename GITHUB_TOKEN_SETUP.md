# 🔑 Настройка GitHub Token для Git синхронизации

## Проблема
```
fatal: could not read Username for 'https://github.com': terminal prompts disabled
```

## Причина
Git требует аутентификацию для push операций, но в Streamlit Cloud нет интерактивного терминала.

## Решение

### Вариант 1: Автоматический токен (рекомендуется)

Streamlit Cloud автоматически предоставляет GitHub token для подключенных репозиториев. Код автоматически использует его.

**Проверьте:**
1. Репозиторий подключен к Streamlit Cloud
2. У приложения есть права на запись

### Вариант 2: Ручная настройка токена

Если автоматический токен не работает:

1. **Создайте Personal Access Token в GitHub:**
   - Settings → Developer settings → Personal access tokens → Tokens (classic)
   - Generate new token (classic)
   - Выберите права: `repo` (полный доступ к репозиториям)
   - Скопируйте токен

2. **Добавьте в Streamlit Cloud Secrets:**
```toml
GITHUB_TOKEN = "ghp_your_token_here"
```

3. **Код автоматически использует токен из переменных окружения**

### Вариант 3: Использование credential helper

Код автоматически настраивает credential helper, но если нужно вручную:

```bash
git config --global credential.helper store
```

## Проверка

После настройки проверьте логи:
- `✅ Remote URL updated with token` - токен добавлен в URL
- `✅ Git push successful!` - синхронизация работает

## Безопасность

⚠️ **ВАЖНО:** Не коммитьте токен в Git!
- Используйте только Streamlit Cloud Secrets
- Токен должен быть в `.gitignore`
- Никогда не публикуйте токен

## Альтернатива

Если токен не работает, рассмотрите:
- Использование облачной БД (PostgreSQL)
- Периодическое резервное копирование через API
- Использование GitHub Actions для синхронизации

