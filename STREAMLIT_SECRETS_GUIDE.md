# 🔑 Руководство по добавлению GitHub Token в Streamlit Cloud Secrets

## Проблема
Не удается добавить GitHub токен в Streamlit Cloud Secrets.

## Решение

### Шаг 1: Создайте Personal Access Token в GitHub

1. Перейдите в GitHub: **Settings → Developer settings → Personal access tokens → Tokens (classic)**
2. Нажмите **"Generate new token (classic)"**
3. Выберите права доступа:
   - ✅ **`repo`** (полный доступ к репозиториям) - ОБЯЗАТЕЛЬНО!
   - ✅ **`workflow`** (опционально, если используете GitHub Actions)
4. Нажмите **"Generate token"**
5. **Скопируйте токен сразу** (он больше не будет показан!)

### Шаг 2: Добавьте токен в Streamlit Cloud Secrets

1. Откройте ваше приложение на **Streamlit Cloud**
2. Перейдите в **Settings → Secrets**
3. Добавьте следующее:

```toml
GITHUB_TOKEN = "ghp_your_token_here"
```

**ВАЖНО:**
- Токен должен быть в кавычках: `"ghp_..."`
- Не используйте пробелы вокруг `=`
- Токен должен начинаться с `ghp_` (для classic tokens)

### Шаг 3: Проверьте формат

Правильный формат:
```toml
GITHUB_TOKEN = "ghp_1234567890abcdefghijklmnopqrstuvwxyz"
```

Неправильные форматы:
```toml
# ❌ Без кавычек
GITHUB_TOKEN = ghp_1234567890abcdefghijklmnopqrstuvwxyz

# ❌ С пробелами вокруг =
GITHUB_TOKEN = "ghp_1234567890abcdefghijklmnopqrstuvwxyz"

# ❌ С комментарием на той же строке
GITHUB_TOKEN = "ghp_1234567890abcdefghijklmnopqrstuvwxyz" # мой токен
```

### Шаг 4: Перезапустите приложение

После добавления токена:
1. Сохраните Secrets
2. Перезапустите приложение в Streamlit Cloud
3. Проверьте логи - должно быть: `✅ Remote URL updated with token`

## Альтернатива: Использование автоматического токена

Streamlit Cloud автоматически предоставляет GitHub token для подключенных репозиториев. Код автоматически использует его, если он доступен.

**Проверьте:**
- Репозиторий подключен к Streamlit Cloud
- У приложения есть права на запись в репозиторий

## Проверка работы

После добавления токена проверьте логи Streamlit Cloud:
- ✅ `✅ Remote URL updated with token` - токен найден и используется
- ✅ `✅ Git push successful!` - синхронизация работает
- ❌ `fatal: could not read Username` - токен не работает или не найден

## Безопасность

⚠️ **ВАЖНО:**
- НИКОГДА не коммитьте токен в Git
- Токен должен быть ТОЛЬКО в Streamlit Cloud Secrets
- Не делитесь токеном публично
- Если токен скомпрометирован, немедленно отзовите его в GitHub

## Отзыв токена

Если нужно отозвать токен:
1. GitHub → Settings → Developer settings → Personal access tokens
2. Найдите токен в списке
3. Нажмите **"Revoke"**

## Troubleshooting

### Проблема: Токен не работает
**Решение:**
- Проверьте, что токен имеет права `repo`
- Убедитесь, что токен не истек
- Проверьте формат в Secrets (должны быть кавычки)

### Проблема: "Push cannot contain secrets"
**Решение:**
- Это нормально - GitHub защищает от утечки токенов
- Токен должен быть только в Secrets, не в коде

### Проблема: Токен не читается из Secrets
**Решение:**
- Проверьте имя переменной: `GITHUB_TOKEN` (заглавными буквами)
- Убедитесь, что приложение перезапущено после добавления Secrets
- Проверьте логи на наличие ошибок

