# 🔧 Исправление проблемы с Git синхронизацией в Streamlit Cloud

## Проблема
Данные не сохраняются после перезапуска - Git синхронизация не работает.

## Возможные причины

### 1. Git синхронизация отключена
**Решение:** Проверьте Secrets в Streamlit Cloud:
```toml
GIT_SYNC_ENABLED = "true"
```

### 2. Нет прав на запись в Git
**Проблема:** Streamlit Cloud не может пушить изменения в репозиторий.

**Решение:**
1. Убедитесь, что репозиторий подключен к Streamlit Cloud
2. Проверьте, что у приложения есть права на запись
3. В настройках репозитория GitHub разрешите доступ для Streamlit Cloud

### 3. Git не настроен в контейнере
**Проблема:** В Streamlit Cloud может не быть Git или он не настроен.

**Решение:** Добавьте в Secrets:
```toml
GIT_USER_NAME = "Streamlit Cloud"
GIT_USER_EMAIL = "streamlit@cloud.com"
```

### 4. База данных слишком большая
**Проблема:** Git плохо работает с большими бинарными файлами.

**Решение:** Рассмотрите использование облачной БД (PostgreSQL).

## Диагностика

### Проверка логов Streamlit Cloud

Ищите в логах:
- `✅ Successfully synced to Git` - успешно
- `❌ Git push failed` - ошибка push
- `Git sync is disabled` - синхронизация отключена
- `Not a git repository` - не Git репозиторий

### Ручная проверка

1. Добавьте пациента/прием
2. Подождите 3-5 секунд
3. Проверьте коммиты в GitHub - должны быть новые коммиты
4. Если коммитов нет - проверьте логи

## Альтернативное решение

Если Git синхронизация не работает, используйте:

### Вариант 1: Облачная база данных
```python
# Используйте PostgreSQL вместо SQLite
DATABASE_URL = "postgresql://user:pass@host/db"
```

### Вариант 2: Периодическое резервное копирование
```python
# Автоматическое сохранение каждые N минут
# Используйте внешнее хранилище (S3, GCS)
```

### Вариант 3: Webhook для синхронизации
```python
# Отправляйте изменения через API в отдельный сервис
# Который будет коммитить в Git
```

## Быстрая проверка

Запустите в Streamlit Cloud:
```python
from git_sync import sync_database_to_git_sync
result = sync_database_to_git_sync("Manual test", push=True)
print(f"Sync result: {result}")
```

Если `result = False`, проверьте логи для деталей ошибки.

