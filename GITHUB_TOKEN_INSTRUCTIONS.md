# 🔑 Инструкция: Как добавить GitHub Token в Streamlit Cloud

## 📍 Где добавить токен

**GitHub токен нужно добавить в Streamlit Cloud Secrets**, а НЕ в файлы проекта!

## 🚀 Пошаговая инструкция

### Шаг 1: Откройте Streamlit Cloud

1. Перейдите на https://share.streamlit.io/
2. Войдите в свой аккаунт
3. Найдите ваше приложение `jardem-medical-center`
4. Нажмите на него

### Шаг 2: Откройте Settings → Secrets

1. В правом верхнем углу нажмите на **три точки (⋮)** или **⚙️ Settings**
2. В меню выберите **"Secrets"** или **"Secrets management"**
3. Откроется редактор секретов

### Шаг 3: Добавьте токен

В редакторе секретов добавьте следующую строку:

```toml
GITHUB_TOKEN = "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"
```

**ВАЖНО:**
- ✅ Токен должен быть в **кавычках** (`"..."`)
- ✅ Название переменной: `GITHUB_TOKEN` (заглавными буквами)
- ✅ Не используйте пробелы вокруг `=`
- ✅ Не добавляйте комментарии на той же строке

### Шаг 4: Сохраните и перезапустите

1. Нажмите **"Save"** или **"Save secrets"**
2. Перезапустите приложение:
   - Нажмите **"⋮** → **"Reboot app"** или
   - Подождите автоматического перезапуска

## ✅ Проверка

После добавления токена и перезапуска, в логах должно появиться:

```
✅ GitHub token found in Streamlit secrets
✅ Remote URL updated with token
✅ Git push successful!
```

Если токен не найден, вы увидите:
```
⚠️ GITHUB_TOKEN not found in Streamlit secrets
```

## 🔍 Где НЕ нужно добавлять токен

❌ **НЕ добавляйте токен в:**
- `.streamlit/secrets.toml` (локальный файл)
- `.streamlit/secrets.toml.example` (пример файла)
- Любые другие файлы в Git репозитории
- Переменные окружения локально

✅ **Токен должен быть ТОЛЬКО в Streamlit Cloud Secrets!**

## 📸 Визуальная инструкция

1. **Streamlit Cloud Dashboard** → Ваше приложение
2. **Settings** (⚙️) → **Secrets**
3. В редакторе добавьте:
   ```
   GITHUB_TOKEN = "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"
   ```
4. **Save** → **Reboot app**

## 🆘 Если не работает

1. Проверьте формат токена (должен быть в кавычках)
2. Убедитесь, что название переменной: `GITHUB_TOKEN` (заглавными)
3. Перезапустите приложение
4. Проверьте логи - там будет показано, какие ключи доступны в secrets

## 📝 Пример правильного формата

```toml
# Правильно ✅
GITHUB_TOKEN = "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"

# Неправильно ❌
GITHUB_TOKEN = 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  # без кавычек
GITHUB_TOKEN="ghp_..."  # без пробелов (может не работать)
github_token = "ghp_..."  # маленькими буквами
```

## 🔐 Безопасность

⚠️ **ВАЖНО:**
- Токен должен быть **только** в Streamlit Cloud Secrets
- **НЕ коммитьте** токен в Git
- **НЕ делитесь** токеном публично
- Если токен скомпрометирован, отзовите его в GitHub

